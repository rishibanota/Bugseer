# Core runtime (everything BugSeer needs to run fully offline)
typer>=0.12
rich>=13.0
pydantic>=2.0
networkx>=3.0
jinja2>=3.1
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3

# --- Optional extras -------------------------------------------------------
# Multi-language parsing (Phase 1). Without these, Python is parsed with the
# stdlib `ast` and other languages use the built-in heuristic analyzer.
tree-sitter>=0.21
tree-sitter-language-pack>=0.2

# Dashboard server (Phase 4)
fastapi>=0.110
uvicorn[standard]>=0.27

# Optional .env loading for the optional AI narrator
python-dotenv>=1.0

# Optional stronger ML backends (Phase 3)
# xgboost>=2.0
# lightgbm>=4.0
